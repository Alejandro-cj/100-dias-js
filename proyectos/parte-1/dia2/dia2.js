// Operadores
let suma = 3 + 5; // Suma
let resta = 10 - 3; // Resta
let multiplicacion = 4 * 7; // Multiplicación
let division = 20 / 4; // División

// Función que saluda al usuario
function saludar(nombre) {
  console.log(`¡Hola, ${nombre}!`);
}

// Llamada a la función de saludo
saludar("Juan");
